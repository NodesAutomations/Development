### Shapes
- Use Merge shape functionality to add or remove parts
- Press `CTRL` while resizing to keep center point fixed
- Press `Shift` while resizing to keep aspect ratio
- Press `CTRL`+`Shift` to keep center point and aspect ratio